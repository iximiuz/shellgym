# Package tools

Software on Linux is installed through a package manager, and which one
depends on the distribution family: `apt` on Debian and Ubuntu, `dnf` on
Fedora, Rocky, and friends. Shell Gym shows you only the task that
matches the machine you are on.
