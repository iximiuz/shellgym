# Networking basics

Servers listen on ports; clients connect to them. This module trains the
inspection side (`ss`, `curl`) and the serving side (starting and stopping
a simple listener), enough to debug the everyday "is it up, and on which
port?" questions.
