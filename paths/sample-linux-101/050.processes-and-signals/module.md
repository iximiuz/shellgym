# Processes and signals

Every running program is a process with a numeric ID (PID). You can list
processes, run them in the background, and control them by sending
signals. This module trains the everyday moves: `ps`, `pgrep`, `&`,
`kill`, and reading the process table without drowning in it.
