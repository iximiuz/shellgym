# Look around

`cd` gets you places; `ls` tells you what is there. Plain `ls` shows the
visible names in a directory - and a handful of options turn it into a
full inspection tool: hidden entries, ownership, sizes, timestamps,
ordering.

This module builds the looking-around reflex: arrive, list, know where
you stand.
