# Make your mark

Two commands create nearly all filesystem structure you will ever set
up by hand: `touch` makes an empty file (or refreshes an existing
one's timestamp), and `mkdir` makes directories - whole trees of them
with `-p`.

Small commands, endless use: scaffolding projects, dropping marker
files, preparing directories for other programs to fill.
